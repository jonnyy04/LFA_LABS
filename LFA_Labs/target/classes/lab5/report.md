# Chomsky Normal Form

### Course: Formal Languages & Finite Automata
### Author: Vornicescu Ion

## Theory
Chomsky Normal Form (CNF) is a simplified form of context-free grammar where all production rules must be of one of two forms:
1. A → BC (where A, B, C are non-terminals)
2. A → a (where A is a non-terminal and 'a' is a terminal)

The conversion to CNF involves several key steps:
1. **Eliminate ε-productions** (A → ε)
2. **Eliminate renaming productions** (A → B)
3. **Eliminate inaccessible symbols**
4. **Eliminate non-productive symbols**
5. **Convert remaining productions to CNF**

CNF is important because it simplifies parsing algorithms and enables efficient analysis of context-free grammars.

---

## Objectives
* Learn about Chomsky Normal Form (CNF)
* Get familiar with the approaches of normalizing a grammar
* Implement a method for normalizing an input grammar by the rules of CNF

---

## Implementation Description

The implementation consists of several key methods that transform the grammar step-by-step into CNF. Here are the most important parts:

### 1. Eliminating ε-Productions
```java
private void eliminateEpsilonProductions() {
    Set<String> nullable = new HashSet<>();
    boolean changed;
    do {
        changed = false;
        for (Map.Entry<String, List<List<String>>> entry : productions.entrySet()) {
            String nt = entry.getKey();
            for (List<String> production : entry.getValue()) {
                if (production.equals(Arrays.asList("ε")) {
                    nullable.add(nt);
                    changed = true;
                }
            }
        }
    } while (changed);
  
}
```
This method first identifies all nullable non-terminals (those that can derive ε). Then it generates all possible combinations of productions without the nullable symbols. If the start symbol is nullable, it adds a new start symbol.
it also identifies all nullable non-terminals — symbols that can derive ε (the empty string).
It loops through the grammar to find such rules and builds a set of nullable non-terminals.
Once found, the algorithm can later use these to generate new productions that omit nullable symbols, maintaining correctness.
This step is essential because CNF does not allow ε-productions except possibly for the start symbol.

### Eliminating Renaming Productions
```java
private void eliminateRenaming() {
    do {
        changed = false;
        // For each production A → B, replace with all productions of B
        for (List<String> production : productions.get(nt)) {
            if (production.size() == 1 && nonTerminals.contains(production.get(0))) {
                // Replace with all productions of the right-hand side non-terminal
                changed = true;
            }
        }
    } while (changed);
}
```
This step removes renaming rules of the form A → B, where both sides are non-terminals.
It substitutes each such production by copying all of B’s rules into A's set of productions.
The process is repeated until no more renaming rules are found, ensuring that all derivations go through actual terminal or structured non-terminal outputs.
This prevents unnecessary indirection in the grammar, aligning with CNF requirements.
### Eliminating Inaccessible Symbols
```java
     private void eliminateInaccessibleSymbols() {
    Set<String> accessible = new HashSet<>();
    accessible.add(startSymbol);
    
}   
```
This method performs a reachability analysis starting from the start symbol.
Only non-terminals that can be reached through a sequence of valid productions from the start symbol are retained.
Any unreachable symbols are removed, as they have no impact on the language generated by the grammar.
This simplifies the grammar and ensures only relevant parts are processed in further steps.


### Converting to CNF

```java
private void convertToChomskyNormalForm() {
    // Step 1: Replace terminals with new non-terminals
    Map<String, String> terminalToNonTerminal = new HashMap<>();
    // Step 2: Break long productions into binary productions
    while (exists productions with length > 2) {
        // Split into pairs with new non-terminals
    }
}
```
In this step, any terminal in the middle of a longer production is replaced with a new non-terminal that derives only that terminal.
Next, long productions (with more than two symbols on the right-hand side) are broken into binary productions using helper non-terminals.
The process is repeated until every production is either of the form A → BC or A → a, as required by CNF.
This structured decomposition makes the grammar suitable for parsing algorithms like CYK.

### **Sample Outputs**
```plaintext
After eliminating ε productions:
Non-terminals: [A, B, S, C, E]
Terminals: [a, b]
Start symbol: S
Productions:
A → a | a S
B → A | A C | b S | a A a
S → b A | B
C → A B
E → B A

After eliminating renaming:
Non-terminals: [A, B, S, C, E]
Terminals: [a, b]
Start symbol: S
Productions:
A → a | a S
B → a | a S | A C | b S | a A a
S → b A | a | a S | A C | b S | a A a
C → A B
E → B A

After eliminating inaccessible symbols:
Non-terminals: [A, B, S, C]
Terminals: [a, b]
Start symbol: S
Productions:
A → a | a S
B → a | a S | A C | b S | a A a
S → b A | a | a S | A C | b S | a A a
C → A B

After eliminating non-productive symbols:
Non-terminals: [A, B, S, C]
Terminals: [a, b]
Start symbol: S
Productions:
A → a | a S
B → a | a S | A C | b S | a A a
S → b A | a | a S | A C | b S | a A a
C → A B

Final CNF:
Non-terminals: [A, B, S, C, Y1, Y2, X1, X2]
Terminals: [a, b]
Start symbol: S
Productions:
A → a | Y1 S
B → Y1 X1 | a | Y1 S | A C | Y2 S
S → Y1 X2 | Y2 A | a | Y1 S | A C | Y2 S
C → A B
Y1 → a
X1 → A Y1
Y2 → b
X2 → A Y1
```

The final result demonstrates a correct and complete transformation of the original grammar into Chomsky Normal Form. Each intermediate stage shows a consistent reduction in complexity—first by removing ε-productions and renaming rules, then by filtering out inaccessible and non-productive symbols. The final CNF introduces new helper non-terminals (Y1, Y2, X1, X2) to represent terminals and binary combinations, as required by CNF rules. Every production now conforms to either the form A → BC or A → a, making the grammar suitable for parsing algorithms like CYK. The preservation of the original language is evident, as the structure and derivation paths are maintained despite the normalization. Overall, the transformation is systematic, lossless, and efficiently prepares the grammar for formal analysis.

---

## Conclusion
This laboratory work successfully implemented the transformation of a context-free grammar into Chomsky Normal Form (CNF), following a structured sequence of normalization steps. The process involved eliminating ε-productions, renaming rules, inaccessible symbols, and non-productive symbols, each of which simplified and refined the grammar while preserving its generative capacity. The final step — converting the grammar into strict CNF form — introduced new non-terminals to ensure all productions were binary or terminal-only, which is essential for compatibility with parsing algorithms like CYK.  Through this exercise, we demonstrated not only a theoretical understanding of formal language principles but also their practical application via algorithmic implementation. The step-by-step transformation helps ensure clarity and correctness in grammar simplification, making the final CNF both efficient for computation and structurally consistent. Additionally, this approach highlights the importance of preprocessing in compiler design and formal language analysis. The outcome affirms the power of CNF as a standardized form that facilitates parsing, verification, and other automated reasoning tasks on context-free grammars.
## References
* Regular Expressions - Formal Language Theory
* Finite State Machines & Regular Expressions
* LFPC Guide

